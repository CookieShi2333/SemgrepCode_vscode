##规则包名
java.servlets.security.audit.url-rewriting.url-rewriting
##规则ID
url-rewriting

# java.servlets.security.audit.url-rewriting.url-rewriting

## 规则名称
java.servlets.security.audit.url-rewriting.url-rewriting

## 规则描述
URL rewriting has significant security risks. Since session ID appears in the URL, it may be easily seen by third parties.


### 规则描述中文版
URL 重写存在明显安全风险：会话 ID 出现在 URL 中，易被第三方获取。

### 关联CWE
- CWE-200: Exposure of Sensitive Information to an Unauthorized Actor
